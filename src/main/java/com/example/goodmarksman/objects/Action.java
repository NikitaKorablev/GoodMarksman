package com.example.goodmarksman.objects;

public enum Action {
    GAME_STARTED,
    GAME_STOPPED,
    CLIENT_CONNECTED,
    CONNECT,
    CONNECTION_ERROR,
    CLIENT_STATE,
    UPDATE_GAME_STATE,
    SET_NAME,
    WIDTH_INIT,
    SHOT
}
