package com.example.goodmarksman.objects;

public enum MsgAction {
    GAME_START,
    GAME_PAUSE,
    GAME_STOPPED,
    CLIENT_CONNECTED,
    CONNECT,
    CONNECTION_ERROR,
    CLIENT_STATE,
    UPDATE_GAME_STATE,
    SET_NAME
}
